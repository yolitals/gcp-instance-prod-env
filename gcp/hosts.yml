all:
  children:
    web_server:
      hosts:
        35.225.39.52
      vars:
        ansible_connection: 'ssh'
        ansible_user: 'ubuntu'
        ansible_ssh_private_key_file: './credentials/.credentials/ssh_key'
 